services:
  migrate:
    volumes:
      - ./:/app

  api:
    command: uvicorn creatory_core.main:app --host 0.0.0.0 --port 8000 --reload
    volumes:
      - ./:/app
    environment:
      APP_ENV: development
      API_WORKERS: 1

  agent-orchestrator:
    command: python -m creatory_core.worker
    volumes:
      - ./:/app
    environment:
      APP_ENV: development

  frontend:
    build:
      context: ./creatory_studio
      dockerfile: Dockerfile
      target: deps
    command: sh -c "npm install && npm run dev"
    volumes:
      - ./creatory_studio:/app
      - frontend_node_modules:/app/node_modules
    environment:
      NEXT_PUBLIC_API_URL: http://localhost:8000/api/v1

volumes:
  frontend_node_modules:
