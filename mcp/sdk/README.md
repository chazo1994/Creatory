# MCP SDK (Internal)

This folder documents the internal contract between `creatory_core` and external MCP servers.

The first bootstrap phase keeps the runtime lightweight and focuses on:

- stable JSON request/response envelopes,
- explicit tool schemas,
- traceable invocation metadata.
