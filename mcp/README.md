# MCP Toolbox

This directory hosts the Model Context Protocol surface for Creatory.

- `servers/`: implementation-specific MCP server runtimes.
- `registry/`: discovery manifests and schema definitions.
- `sdk/`: helper contracts and integration notes for contributors.

`registry/default_manifest.yaml` is the bootstrap catalog used by the API in early phases.
